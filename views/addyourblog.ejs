<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <style>
        body {
            background-color: black;
            color: white; /* Optional: change text color to white */
            margin-top: 56px; /* Space for fixed navbar */
            text-align: center; /* Center the text */
        }

        h1 {
            margin-bottom: 20px; /* Space below the heading */
        }

        .container {
            max-width: 600px; /* Set a max width for the form container */
            margin: auto; /* Center the container */
            background-color: #333; /* Optional: Background for better contrast */
            padding: 20px; /* Padding inside the container */
            border-radius: 8px; /* Rounded corners */
        }

        .btn-submit {
            padding: 10px 20px;
            background-color: #37c263; /* Button color */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px; /* Increase button font size */
            transition: background-color 0.3s; /* Smooth background transition */
        }

        .btn-submit:hover {
            background-color: #2da953; /* Darker shade on hover */
        }
    </style>
</head>

<body>
    <%- include('./partials/nav') %>

    <div class="container mt-3">
        <h1>Add Your Blog</h1> <!-- Added heading for clarity -->
        <form action="/blog" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="coverImage" class="form-label">Cover Image</label>
                <input name="coverImageURL" type="file" class="form-control" id="coverImage" aria-describedby="coverImage">
            </div>
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input name="title" type="text" class="form-control" id="title" aria-describedby="title">
            </div>
            <div class="mb-3">
                <label for="body" class="form-label">Body</label>
                <textarea name="body" class="form-control" placeholder="Leave a comment here" id="body"></textarea>
            </div>
            <div class="mb-3">
                <button type="submit" class="btn-submit">Submit</button> <!-- Added button class -->
            </div>
        </form>
    </div>

    <%- include('./partials/script') %>
</body>

</html>
