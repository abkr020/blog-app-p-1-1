<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <title>Profile Page</title> <!-- Updated title for clarity -->
    <style>
        body {
            background-color: black;
            color: white;
            margin-top: 56px; /* Space for fixed navbar */
        }

        h1 {
            text-align: center; /* Center the heading */
            margin: 20px 0; /* Space above and below the heading */
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            margin: 0 auto; /* Center the card container */
            max-width: 1200px; /* Optional: Set a max width for better layout */
        }

        .card {
            margin: 10px; 
            width: 18rem; /* Fixed width for each card */
            background-color: #1c1c1c; /* Optional: Background color for cards */
            color: white;

            border-radius: 5px; /* Optional: Rounded corners */
            overflow: hidden; /* Hide overflow from images */
        }

        .card img {
            height: 200px; /* Set a fixed height */
            width: 100%; /* Allow the width to adjust */
            object-fit: contain; /* Ensures the entire image is visible */
        }

        .card-body {
            padding: 15px; /* Add some padding to the card body */
        }
        /* didnt check this .card-title and .card-text working or not because i dont want to login check it later and also add a dumy account for easy login without otp and simple login to test */
        .card-title {
            max-height: 3em; /* Limit title height (approx. 1.5 lines) */
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .card-text {
            display: -webkit-box; /* Enable truncation for multiple lines */
            -webkit-line-clamp: 3; /* Show only 3 lines */
            -webkit-box-orient: vertical;
            overflow: hidden;
            text-overflow: ellipsis;
        }

    </style>
</head>

<body>
    <%- include('./partials/nav') %>

    <h1>Profile Page</h1> <!-- Added a heading for clarity -->

    <div class="card-container">
        <% blogs.reverse().forEach(blog => { %>
            <div class="card">
                <img src="<%= blog.coverImageURL %>" class="card-img-top" alt="<%= blog.title %>"> <!-- Added alt attribute -->
                <div class="card-body">
                    <h5 class="card-title">
                        <%= blog.title %>
                    </h5>
                    <p class="card-text">
                        <%= blog.body %>
                    </p>
                    <a href="/blog/view/<%= blog._id %>" class="btn btn-primary">View</a>
                    <a href="/blog/edit/<%= blog._id %>" class="btn btn-primary">Edit</a>
                </div>
            </div>
        <% }) %>
    </div>

    <% if (locals.user) { %>
        <div class="container mt-4">
            <div class="alert alert-info" role="alert">
                Logged in as: <%= JSON.stringify(user.fullname) %> <!-- Display the user's full name -->
            </div>
        </div>
    <% } %>

    <%- include('./partials/script') %>
</body>

</html>
